const AJS = require("../src/index");

describe('AJS.dom', () => {
  describe('E.once', () => {
    test('should bind window hashchange event', () => {
      // AJS.dom.E.once('window.hashchange', () => {})
    });
  });
});
